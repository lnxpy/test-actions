on:
  push:
    branches: [ main ]
  workflow_dispatch:
  
  jobs:
  repo-sync:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        persist-credentials: false
    - name: repo-sync
      uses: shipyard/github-action-python-template
      with:
#         num: 5
